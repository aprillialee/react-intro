import React from "react";
import {
  storiesOf
} from "@storybook/react";

import LoginButton from "../LoginButton"

storiesOf("Primitives|LoginButton", module)
  .add("Login", () => ( < LoginButton > LOGIN < /LoginButton>)

    )